import { msg as topLevel } from '#top-level';
import { msg as sameLevel } from '#same-level';
import { msg as nested } from '#nested/path.js';
import { msg as dotPrefixed } from '#dot-prefixed';
import query from '#query';
export { topLevel, sameLevel, nested, dotPrefixed, query };